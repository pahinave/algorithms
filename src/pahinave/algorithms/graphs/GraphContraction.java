package pahinave.algorithms.graphs;

public class GraphContraction {
	public <T,S> void contract(Graph<T,S> graph) {
		// while there are more than 2 vertices
		//	randomly choose edge
		//	fuse the endpoints of the edge
		//		create a new node with name combination of both nodes
		//		for all edges outbound from end point nodes, add those edges to new node
	}
}
